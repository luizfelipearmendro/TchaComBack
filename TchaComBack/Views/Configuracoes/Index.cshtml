@model List<ConfiguracoesModel>
@{
    ViewData["Title"] = "Configurações";
}

<link href="~/css/Home/home.css" rel="stylesheet" />
<link href="~/css/Configuracoes/configuracoes.css" rel="stylesheet" />

@if (TempData["MensagemSucesso"] != null)
{
    <div id="successAlert" class="custom-alert success">
        <span><strong>Sucesso!</strong> @TempData["MensagemSucesso"]</span>
        <span class="closebtn" onclick="closeAlert('successAlert')">&times;</span>
    </div>
    <script>
        setTimeout(() => {
        document.getElementById("successAlert").style.display = 'none';
        }, 5000);
    </script>
}

@if (TempData["MensagemErro"] != null)
{
    <div id="errorAlert" class="custom-alert error">
        <span><strong>Atenção!</strong> @TempData["MensagemErro"]</span>
        <span class="closebtn" onclick="closeAlert('errorAlert')">&times;</span>
    </div>
    <script>
        setTimeout(() => {
        document.getElementById("errorAlert").style.display = 'none';
        }, 5000);
    </script>
}

<h3>Configurações</h3>
<a asp-controller="Configuracoes" asp-action="Criar" class="btn btn-primary mb-3 botao-primario" style="margin-top: 1rem;">Nova Configuração</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <th class="text-center" style="width: 25%;">Nome</th>
            <th class="text-center">Início</th>
            <th class="text-center">Intervalo</th>
            <th class="text-center">Fim</th>
            <th class="text-center">Status</th>
            <th class="text-center">Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.NomeConfiguracao</td>
                <td class="text-center">@item.InicioExpediente</td>
                <td class="text-center">@item.IntervaloInicio - @item.IntervaloFim</td>
                <td class="text-center">@item.FimExpediente</td>
                <td class="text-center">
                    @if (item.Ativo)
                    {
                        <span class="badge bg-success">Ativa</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Inativa</span>
                    }
                </td>
                <td class="text-center">
                    @if (!item.Ativo)
                    {
                        <a class="btn btn-sm btn-primary" href="@Url.Action("Ativar", new { id = item.Id })">Ativar</a>
                    }
                    else
                    {
                        <a class="btn btn-sm btn-danger" href="@Url.Action("Desativar", new { id = item.Id })">Desativar</a>
                    }
                    <a class="btn btn-sm btn-secondary" href="@Url.Action("Editar", new { id = item.Id })">Editar</a>
                </td>
            </tr>
        }
    </tbody>
</table>